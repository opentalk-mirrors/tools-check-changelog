# `Check Changelog Bot` – Changelog Management Tools

This repository provides a set of tools designed to streamline the maintenance
of our changelogs.

Since the changelog is generated based on the commit history, commit messages
follow the [Conventional Commits][ConCom] specification.

Creating the changelog is done using a customized version of [`git-cliff`](https://git-cliff.org/).

Check the [commit message format reference][commit-format] for a guide on how to write commit messages.

## Reviewing Changelog Entries

To ensure the changelog is properly formatted, commit messages must conform to
the [Conventional Commits][ConCom] specification. This is enforced via a linting
step in our CI/CD pipeline.

The following GitLab CI/CD job verifies the commits in a merge request are valid:

```yaml
lint:commit:
  cache: {}
  image: node:lts-alpine
  stage: lint
  <<: *default_job_condition
  variables:
    GIT_DEPTH: 250
  before_script:
    - apk add --no-cache git
    - npm install -g @commitlint/cli @commitlint/config-conventional
  script:
    - npx commitlint --from $CI_MERGE_REQUEST_DIFF_BASE_SHA --to HEAD --verbose
```

Note that [`GIT_DEPTH`](https://docs.gitlab.com/ee/ci/runners/configure_runners.html#shallow-cloning)
is set to 250 but can be adjusted for each project. If a merge request contains
more commits than `GIT_DEPTH`, the job will fail because the full commit history
won’t be available in the cloned repository used by the job.

A changelog bot will automatically open a thread in each merge request, displaying
the changelog entries generated by that merge request. The thread must be resolved
before the merge request can be merged.

The bot is triggered with the following CI job:

```yaml
trigger_changelog_preview:
  stage: trigger
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
  variables:
    PIPELINE_PROJECT_PATH: $CI_PROJECT_PATH
    PIPELINE_MERGE_REQUEST_PROJECT_PATH: $CI_MERGE_REQUEST_SOURCE_PROJECT_PATH
    PIPELINE_MERGE_REQUEST_SOURCE_BRANCH_NAME: $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME
    PIPELINE_MERGE_REQUEST_IID: $CI_MERGE_REQUEST_IID
  trigger: opentalk/tools/check-changelog
```

## Building the Changelog

Make sure to install our custom [`git-cliff`][custom-cliff] before running the script:

```bash
cargo install --git ssh://git@git.opentalk.dev:222/opentalk/tools/git-cliff.git git-cliff
```

The changelog can be updated with:

```bash
GITLAB_TOKEN=(cat ~/.gitlab_token) \
GITLAB_API_URL=https://git.opentalk.dev/api/v4 \
GITLAB_REPO="path/to/project" \
git-cliff \
        --config opentalk \
        --unreleased \
        --prepend CHANGELOG.md \
        --tag "v$VERSION"
```

make sure to:

1. have a valid `GITLAB_TOKEN` with `read` scope
2. set the `GITLAB_REPO` to the full project name (e.g. `opentalk/tools/retoki`)
3. set the version number

[ConCom]: https://www.conventionalcommits.org/en/v1.0.0/
[Just]: https://just.systems/man/en/introduction.html
[custom-cliff]: https://git.opentalk.dev/opentalk/tools/git-cliff
[commit-format]: ./doc/commit-message-format.md
